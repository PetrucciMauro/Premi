{
  "name": "Premi",
  "version": "0.0.1",
  "description": "Premi - Better than Prezi",
  "author": "LateButSafe",
  "license": "MIT",  

  "repository": {
  "type":"git",
  "url": "https://github.com/PetrucciMauro/sourceAngular.git"
  },

  "keywords":[
   "LateButSafe",
   "Presentazioni",
   "Presentation",
   "SlideShow"

  ],

  "devDependencies": {
    "karma": "^0.12.16",
    "karma-jasmine": "^0.1.5",
    "http-server": "^0.6.1",
    "tmp": "0.0.23",
    "bower": "^1.3.1",
    "shelljs": "^0.2.6",
    "karma-phantomjs-launcher":"latest",
    "mocha": "latest",
    "istanbul":"latest",
    "angular-route": "~1.3.0",
    "coveralls":"latest",
    "jshint": "^2.7.0",
    "mocha": "^2.2.4",
    "mocha-lcov-reporter": "^0.0.2",
    "mocha-istanbul": "^0.2.0",
    "karma-coverage": "latest"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000",
    "pretest": "npm install",
    "test": "./node_modules/.bin/karma start test/karma.conf.js --single-run --browsers PhantomJS --ignore-errors",
    "test-travis": "./node_modules/istanbul/lib/cli.js cover ./node_modules/.bin/karma start test/karma.conf.js -- -R spec ./test/unit/*",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",
    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor test/protractor-conf.js"
  }
}
